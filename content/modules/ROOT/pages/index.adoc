= Build an AI Tool Calling System from Scratch

Welcome to the *vLLM Tool Calling on OpenShift AI* lab. In this hands-on workshop you will build a complete tool calling system -- from understanding the protocol, to deploying a model, to writing an automated multi-tool application. Everything runs on {intel_platform} processors.

== What You Will BUILD

By the end of this lab you will have constructed:

* A *vLLM model server* with tool calling enabled, serving Granite 3.2-8B or Qwen3-14B
* The complete *6-step tool calling protocol*, executed by hand so you understand every piece
* An *automated dispatch loop* in Python that handles the tool call lifecycle
* A *multi-tool application* where the model chains multiple function calls to answer complex questions
* *Advanced patterns* including parallel calls, error handling, and guard rails

There is no application UI. You build and run everything from the terminal using `curl` and Python.

== The Tool Calling Lifecycle

Tool calling follows a precise protocol. Every step in this lab maps to one part of the lifecycle:

[cols="1,2,1"]
|===
| Phase | What Happens | Module

| *Define*
| You describe available functions in JSON Schema
| 3

| *Request*
| The model returns a `tool_calls` array instead of text
| 3

| *Execute*
| Your code runs the function and collects results
| 3-4

| *Return*
| You send the result back as a `role: "tool"` message
| 3-4

| *Synthesize*
| The model incorporates the result into a natural answer
| 3-4

| *Chain*
| The model calls multiple tools in sequence to solve complex problems
| 5
|===

The model never executes code. It produces structured JSON requests. Your application handles execution, error handling, and returning results.

== Architecture

[cols="1,3"]
|===
| Component | Purpose

| *vLLM with Tool Calling*
| Serves a tool-calling-capable model with `--enable-auto-tool-choice`. Exposes an OpenAI-compatible `/v1/chat/completions` endpoint.

| *OpenShift AI*
| Provides the serving runtime, resource management, and networking.

| *Your Terminal*
| Where you build `curl` commands and Python scripts that interact with the model API.
|===

== Time Estimate

This lab takes approximately *90 minutes* to complete:

[cols="1,3,1"]
|===
| Module | Topic | Time

| 1 | Tool Calling Fundamentals | 10 min
| 2 | Serve a Model with Tool Calling | 15 min
| 3 | Anatomy of a Tool Call | 20 min
| 4 | Build the Tool Calling Loop | 20 min
| 5 | Build a Multi-Tool Application | 15 min
| 6 | Advanced Patterns | 10 min
| 7 | The Intel Xeon Story | 5 min
| 8 | Conclusion | 5 min
|===

[NOTE]
====
Each module builds on the previous one. Work through them in order. By the end you will understand tool calling well enough to build your own agentic AI systems.
====

== Ready?

When you are ready, proceed to xref:01-tool-calling-fundamentals.adoc[Module 1: Tool Calling Fundamentals] to learn what tool calling is and why it matters.
